apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: flix
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30090
      protocol: TCP
  type: NodePort
